<Window x:Class="ReceptionFrontend.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hotel Concierge Dashboard" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Live Guest Requests" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid x:Name="RequestsGrid" Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  ItemsSource="{Binding}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Room" Binding="{Binding RoomNumber}" Width="80"/>
                <DataGridTextColumn Header="Type" Binding="{Binding RequestType}" Width="150"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                <DataGridTextColumn Header="Time" Binding="{Binding CreatedAt, StringFormat=\{0:HH:mm\}}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>